/**
 * @file Firebase Security Rules for the Finanças app.
 *
 * @corePhilosophy This ruleset enforces a strict user-ownership model, ensuring that users can only
 *                 access and manipulate data associated with their own accounts.
 *
 * @dataStructure The database is structured with user profiles stored in `/users/{userId}` and
 *                individual transaction records stored in the `/users/{userId}/transactions/{transactionId}`
 *                subcollection. This design enforces clear ownership and simplifies access control.
 *
 * @keySecurityDecisions
 *   - Users can only read and write their own user profile data.
 *   - Users can only create, read, update, and delete transactions associated with their own user ID.
 *   - Users can manage their own expense groups.
 *   - Users can manage their own predefined descriptions.
 *   - Listing all users is disallowed to protect user privacy.
 *   - Data validation is relaxed to allow for rapid prototyping, focusing on ownership and
 *     relational integrity checks rather than exhaustive schema validation.
 *
 * @denormalizationForAuthorization Not applicable in this configuration.
 *
 * @structuralSegregation User profiles and transactions are stored in a hierarchical structure,
 *                       segregating data based on user ownership. This approach is more secure and
 *                       performant than using a single collection with a user ID field.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isOwner(userId) {
      return request.auth.uid == userId;
    }

    function isSignedIn() {
      return request.auth != null;
    }

    /**
     * @description Secures the user profile document.
     * @path /databases/{database}/documents/users/{userId}
     */
    match /users/{userId} {
      allow get, update, delete: if isSignedIn() && isOwner(userId);
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow list: if false;

      /**
       * @description Secures the user's transaction subcollection.
       * @path /databases/{database}/documents/users/{userId}/transactions/{transactionId}
       */
      match /transactions/{transactionId} {
        allow read, write: if isSignedIn() && isOwner(userId);
      }
      
      /**
       * @description Secures the user's groups subcollection.
       * @path /databases/{database}/documents/users/{userId}/groups/{groupId}
       */
      match /groups/{groupId} {
          allow read, write: if isSignedIn() && isOwner(userId);
      }

      /**
       * @description Secures the user's predefined descriptions subcollection.
       * @path /databases/{database}/documents/users/{userId}/predefinedDescriptions/{descriptionId}
       */
      match /predefinedDescriptions/{descriptionId} {
        allow read, write: if isSignedIn() && isOwner(userId);
      }
    }
  }
}
